import { test } from '@playwright/test';

test.describe('Wordpress log in website - user log in options', () => {
    test('User can log in using the wordpress login form', async ({ page }) => {});
});
